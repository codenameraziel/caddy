version: "3.8"
services:
  caddy:
    image: caddy:latest
    container_name: caddy
    volumes:
      - ./caddy:/etc/caddy
      - ./certs:/etc/caddy/certs
      - caddy_data:/data
      - caddy_config:/config
    ports:
      - "80:80"
      - "443:443"

volumes:
  caddy_data:
  caddy_config:
